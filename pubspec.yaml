name: simple_candlestick_chart
description: A simple candlestick chart for flutter. Supports smooth scroll and zoom.
version: 0.0.2
homepage: https://github.com/qiuxiang/simple_candlestick_chart

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
